# Public Site Architecture

## Public route groups
- `/` — Workers runtime redirect entry
- `/home` — primary marketing and creation entry
- `/web/login` — login and registration gate
- `/web/community-view/[slug]` — template preview surface
- `/docs/[slug]` — SEO content pages

## Core building blocks
- Shared `BaseLayout` with optional `hideHeader` and `hideFooter`; it now bootstraps the public locale into `window.__superunPublicLocale`, `document.documentElement.dataset.publicLocale`, and `lang` before page scripts run (`src/layouts/BaseLayout.astro:6`, `src/layouts/BaseLayout.astro:31`)
- Shared `Header`/`LocaleMenu` chrome plus `src/data/fidelity.ts` for the public logo asset, locale list, visible copy, placeholder sequences, and gallery/community media sources (`src/components/Header.astro:1`, `src/components/LocaleMenu.astro:1`, `src/data/fidelity.ts:1`)
- `/home` uses the shared brand-plus-language header, a locale-aware prompt composer hero, a client-side typing/deleting placeholder loop that starts from the first localized phrase fully visible on load, a small JS-triggered hero reveal, page-local template/doc ordering tuned to the live landing-page sequence, real screenshot gallery cards, then footer and doc-link strip in that order; the latest polish pass keeps the hover recreate CTA plus mobile-only preview scaling and leaves `applyCategoryFilter()`, `revealHero()`, and `runPlaceholderLoop()` unchanged while softening the reveal motion and compressing first-screen/composer spacing for closer live parity (`src/pages/home.astro:13`, `src/pages/home.astro:70`, `src/pages/home.astro:97`, `src/pages/home.astro:165`, `src/pages/home.astro:197`, `src/pages/home.astro:204`, `src/pages/home.astro:290`, `src/components/TemplateCard.astro:13`, `src/styles/global.css:221`, `src/styles/global.css:238`, `src/styles/global.css:247`, `src/styles/global.css:259`, `src/styles/global.css:280`, `src/styles/global.css:3635`, `src/styles/global.css:3644`)
- `/web/login` is a focused auth shell with an explicit return-home brand link, shared locale menu, locale-reactive phone/email panels, captured WeChat/Google iconography, and same-site redirect hardening for `redirectUrl`; the page hides the shared site header/footer, keeps the localized title wording aligned to the live public site, makes the revealed Google action replace the `更多` trigger rather than stacking both controls, and tightens auth-shell/card/button spacing to match the target layout more closely (`src/pages/web/login.astro:13`, `src/pages/web/login.astro:16`, `src/pages/web/login.astro:40`, `src/pages/web/login.astro:97`, `src/pages/web/login.astro:135`, `src/pages/web/login.astro:166`, `src/data/fidelity.ts:40`, `src/styles/global.css:597`, `src/styles/global.css:637`, `src/styles/global.css:654`, `src/styles/global.css:692`, `src/styles/global.css:743`, `src/styles/global.css:777`, `src/styles/global.css:880`, `src/styles/global.css:905`)
- `/web/community-view/[slug]` still prerenders every template preview, but only the first three showcase slugs render bespoke local recreations while all remaining slugs continue through remote `communityPreviewSrc`/`templatePageSrc`; both local and iframe-backed routes keep the same immersive full-screen shell, and the iframe branch remains pulse-loaded plus `load`-gated with no extra chrome. The latest local recreation polish leaves that behavior untouched while tightening only page-level fidelity on the three bespoke pages: GlowMind compresses hero spacing and loosens the image/chat-card overlap, Goose compresses summary/question/sidebar density, and Nexus lightens the hero/logo-strip/feature rhythm; the local/iframe split still hinges on `localCommunityPreview`, `iframePreviewSrc`, and `immersivePreview` in the route (`src/data/fidelity.ts:113`, `src/data/fidelity.ts:114`, `src/data/fidelity.ts:119`, `src/data/fidelity.ts:124`, `src/pages/web/community-view/[slug].astro:22`, `src/pages/web/community-view/[slug].astro:90`, `src/pages/web/community-view/[slug].astro:136`, `src/pages/web/community-view/[slug].astro:294`, `src/pages/web/community-view/[slug].astro:295`, `src/pages/web/community-view/[slug].astro:296`, `src/styles/global.css:1786`, `src/styles/global.css:1840`, `src/styles/global.css:1872`, `src/styles/global.css:1900`, `src/styles/global.css:1912`, `src/styles/global.css:2187`, `src/styles/global.css:2250`, `src/styles/global.css:2457`, `src/styles/global.css:2556`, `src/styles/global.css:2856`, `src/styles/global.css:2952`, `src/styles/global.css:3030`, `src/styles/global.css:3035`)
- Shared gallery motion still relies on the hover CTA overlay plus mobile-only preview scaling, while desktop/light and dark cards stay at scale `1` and touch interactions still use the reduced-motion-aware active state (`src/components/TemplateCard.astro:13`, `src/styles/global.css:392`, `src/styles/global.css:435`, `src/styles/global.css:446`, `src/styles/global.css:457`, `src/styles/global.css:463`)
- Workers deployment through `wrangler.jsonc:1` with `assets.directory` pointing at `dist`, the `ASSETS` binding exposed to `src/worker.ts:1`, and `workers.dev` traffic forwarded through that Worker entrypoint; `src/worker.ts:11` immediately redirects `/` to `/home`, and the latest verified public deploy is `https://superun-public-site.sub-store.workers.dev` on version `f20ff594-e748-404f-9162-fa1735f7ebd2`

## Content model
- Template, docs, and shared footer/navigation metadata in `src/data/site.ts`
- Public locale/copy state, placeholder text sequences, brand asset path, and gallery/community media sources in `src/data/fidelity.ts`
- Markdown article bodies in `src/content/docs/`
