# Routes Reference

- `/` — Workers runtime entry immediately 302s to `/home` on public deploys (`src/worker.ts:10`)
- `/home` — landing page with shared brand/language chrome, prompt composer forwarding to `/web/login`, locale-aware visible copy, a client-side typing/deleting placeholder loop that starts with the first localized phrase fully visible on load, a small hero reveal on boot, category filtering, and the upstream runtime gallery spacing. The visible `创建时间` control is intentionally absent because the live runtime hides it, card clicks open hashed `/web/community-view/<preview-slug>` routes in a new tab, cards without a `previewImage` render animated placeholder media shells, and the bottom rail now mirrors the live SEO footer pattern: `最新内容` and `推荐模板` toggle between `/docs/[slug]` links and `/templates/[slug]` links rather than linking into community routes (`src/pages/home.astro:8`, `src/pages/home.astro:17`, `src/pages/home.astro:39`, `src/pages/home.astro:64`, `src/pages/home.astro:83`, `src/pages/home.astro:88`, `src/pages/home.astro:99`, `src/components/TemplateCard.astro:9`, `src/components/TemplateCard.astro:12`, `src/components/TemplateCard.astro:33`, `src/components/BottomLinks.astro:15`, `src/components/BottomLinks.astro:25`, `src/components/BottomLinks.astro:31`, `src/styles/global.css:424`, `src/styles/global.css:4710`)
- `/templates` — public template directory route rendered from the scraped upstream template catalog (`src/pages/templates/index.astro:3`, `src/pages/templates/index.astro:21`)
- `/templates/[slug]` — public template detail route with upstream-aligned title, `介绍`, `模板详情`, and CTA stack; the page remains directly accessible but is no longer the primary destination of `home` card clicks (`src/pages/templates/[slug].astro:3`, `src/pages/templates/[slug].astro:24`, `src/pages/templates/[slug].astro:75`, `src/pages/templates/[slug].astro:83`, `src/data/template-directory.ts:66`)
- `/web/login` — bare auth shell with a button-based return-home brand action plus shared locale menu, phone/email tabs, query-driven prompt carry-through, captured WeChat iconography, and same-site-only `redirectUrl` handling for static deployment; continue actions are rewritten from the sanitized target URL, the page hides the shared site header/footer, the expanded Google action renders with its icon while hiding the `更多` trigger after reveal, no visible footer/bottom-links stack is rendered, and the inputs carry the expected `autocomplete` and `inputmode` hints (`src/pages/web/login.astro:12`, `src/pages/web/login.astro:17`, `src/pages/web/login.astro:54`, `src/pages/web/login.astro:66`, `src/pages/web/login.astro:99`, `src/pages/web/login.astro:142`)
- `/web/community-view/[slug]` — prerendered template preview route using the detailed runtime shell: left panel with project title, parsed long-form intro copy, preview image, and `复刻创作`; right panel with `手机` / `电脑`, `主页端`, and the iframe sourced from `visitUrl`. The route accepts both hashed preview slugs and legacy aliases, and `复刻创作` still goes through `/web/login?redirectUrl=%2Fhome&prompt=...` before landing on `/home?prompt=复刻 ...` (`src/pages/web/community-view/[slug].astro:12`, `src/pages/web/community-view/[slug].astro:21`, `src/pages/web/community-view/[slug].astro:22`, `src/pages/web/community-view/[slug].astro:33`, `src/pages/web/community-view/[slug].astro:53`, `src/pages/web/community-view/[slug].astro:76`, `src/pages/web/community-view/[slug].astro:83`, `src/pages/web/community-view/[slug].astro:92`, `src/pages/web/community-view/[slug].astro:111`, `src/data/template-directory.ts:75`)
- `/docs/[slug]` — prerendered informational article pages backed by `src/data/site.ts` doc records
