# Pass 7 UI Fidelity Epic

## Goal
- Continue from pass 6 and close the next layer of remaining fidelity gaps between the local public site and `https://www.superun.com`.
- Focus on the residual visual details that still read different after pass 6: tighter typography and spacing nuance, gallery/card surface behavior, community shell fidelity, and any remaining desktop/mobile interaction mismatches visible on the deployed site.

## Child Deliverables
1. Refresh pass-7 evidence and identify concrete remaining gaps.
2. Polish homepage residual fidelity gaps.
3. Polish login residual fidelity gaps.
4. Polish community-view residual fidelity gaps.
5. Tune responsive/mobile residual behavior.
6. Tune motion/interaction residual behavior.
7. Validate deploy and sync docs/truth artifacts.

## Constraints
- Keep existing route structure, locale behavior, real brand asset, and remote snapshot mapping intact.
- Prefer focused CSS/markup refinements over architectural rewrites.
- Every child task must validate before being marked done.
- Use the deployed Workers site as the local truth target for each pass comparison.
